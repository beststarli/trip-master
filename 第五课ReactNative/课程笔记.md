# 第五课React Native——个人记录

## App技术方案
- 原生：android/ios
- 跨平台：H5/ReactNative/Flutter等等

## 跨端技术
| 技术方案   | Web 容器              | React Native          | Flutter    |
|------------|-----------------------|-----------------------|------------|
| 开发语言   | html/css/javascript   | Javascript/React      | Dart       |
| 技术栈     | 前端                  | 偏前端                | 偏客户端   |
| 动态能力   | 支持                  | 支持                  | 不支持     |
| 渲染能力   | 差                    | 一般                  | 好         |
| 社区生态   | 活跃                  | 活跃                  | 活跃       |
> 携程使用ReactNative更多一些

ReactNative是Facebook推出的一种开发原生移动应用的框架，它使用类似于React的语法和组件化开发模式，可以同时支持iOS和Android平台。

ReactNative的目标是让开发者只需要掌握JavaScript和React的基础知识，就可以快速地开发出高质量的原生应用。

## ReactNative
### ReactNative优点
- 更适用于前端开发的JavaScript和React开发方案
- HotReload开发简单
- React有着成熟的社区
- 强大的热更新能力
- 官方的持续优化

### ReactNative知识图
![RN知识图](/static/第五课/RN知识图.png)

### ReactNative旧架构
- 客户端的jsc环境中运行的rn业务代码
- 采用android/ios系统渲染UI组件，达成一个原生的体验
- 通过Bridge完成js代码和原生端API和组件的调用
- 在重渲染的交互上体验较差
![RN旧架构](/static/第五课/RN旧架构.png)

### ReactNative新架构
JSI：JavaScript Interface支持JS对象持有C++的引用，同步调用
![RN新架构](/static/第五课/RN新架构.png)

### ReactNative核心组件
| **React Native UI 组件** | **ANDROID 原生视图** | **iOS 原生视图**     | **WEB 标签**              | **说明**                                                             |
|---------------------------|-----------------------|-----------------------|----------------------------|----------------------------------------------------------------------|
| `<View>`                  | `<ViewGroup>`         | `<UIView>`            | `<div>`                    | 一個支持使用 flexbox 佈局、樣式、一些觸摸處理和無障礙性控件的容器     |
| `<Text>`                  | `<TextView>`          | `<UITextView>`        | `<p>`                      | 顯示、樣式和嵌套文本字符串，甚至至處理觸摸事件                         |
| `<Image>`                 | `<ImageView>`         | `<UIImageView>`       | `<img>`                    | 顯示不同類型的圖片                                                   |
| `<ScrollView>`            | `<ScrollView>`        | `<UIScrollView>`      | `<div>`                    | 一個通用的滾動容器，可以包含多個組件和視圖                             |
| `<TextInput>`             | `<EditText>`          | `<UITextField>`       | `<input type="text">`      | 使用戶可以輸入文本                                                   |
### ReactNative基础
#### 样式
推荐使用样式对象
```jsx
const styles = StyleSheet.create({
    layout: {
        margin: 30
    },
    text: {
        fontSize: 20,
        color: 'red'
    },
    itemLayout: {
        marginVertical: 20
    }
})
```

#### 布局
- 使用Yoga布局引擎
- 主要布局方式是：Flexbox
- flex Direction默认为垂直排序column
- 使用justifyContent和alignItems控制子元素在主轴和次轴的排列方式
- position只支持relative和absolute
- 宽高可设为自适应、百分比、固定值

#### props
组件的静态属性，是js的对象，支持传入：
- 基础数据类型
- 函数
- 字组件

静态属性不可变，外部属性变更会引起组件的渲染

#### state
**可以计算出来的不是状态**

### ReactNative交互
#### 触摸事件
常见的触摸手势：点击（长按、双击）、滑动
- 事件处理与Web开发中的事件处理非常相似
- 事件冒泡和捕获遵循DOM标准
- 一般用Touchable系列组件响应事件

#### 动画
**Animated**：
- 声明的形式是来定义动画的输入与输出
- 支持组合、跟踪状态值、跟踪手势

**LayoutAnimation**：
- 全局范围
- 更新flexbox布局

#### 导航
- 使用官方推荐的导航库React Navigation
- React Navigation提供了简单易用的跨平台导航方案
- 提供多种导航组件（翻页、tab选项卡、抽屉）

### ReactNative网络请求
```jsx
export default App = () => {
    const [isLoading, setLoading] = useState([])
    const [data, setData] = useState([])
    useEffect(() => {
        fetch('http://reactnative.dev/movie.json')
        .then((json) => setData(json.movies))
        .catch((error) => console.error(error))
        .finally(() => setLoading(false))
    }, [])

    return (
        <View style={flex: 1, padding: 24}>
            {isLoading ? <ActivityIndicator /> : (
                <FlatList data={data} keyExtractor={({id}, index) => id} renderItem={({item}) => (
                    <Text>{item.title},{itme.releaseYear}</Text>
                )}/>
            )}
        </View>
    )
}
```
### ReactNative虚拟DOM
JSX运行的基础
![虚拟DOM](/static/第五课/虚拟DOM.png)

### ReactNative生命周期
- 组件生命周期
- 响应式Effect生命周期
- 页面生命周期 

![类组件](/static/第五课/类组件.png)
> 类组件

![函数组件](/static/第五课/函数组件.png)
> 函数组件

### ReactNative组件化
基于组件（函数/类组件）的架构模式
- 原生组件（宿主组件）
- 自定义组件
- 第三方组件

### ReactNative开发工具与调试
- flipper
- React Native DevTools
- LogBox

### ReactNative状态管理
- Redux
- Rematch
- Recoil
- RTK

| 特性           | Redux | Rematch | Redux-Toolkit | Recoil | MobX |
|----------------|-------|---------|---------------|--------|------|
| 官方/生态丰富  | ✓     | ✗       | ✓             | ✗      | ✗    |
| TS友好         | ✓     | ✓       | ✓             | ✓      | ✓    |
| 使用人数       | 高    | 低      | 高            | 低     | 低   |
| 编程范式       | 函数式 | 函数式  | 函数式        | 原子+选择器 | 响应式 |
| 冗余/模版代码  | 多    | 低      | 低            | 低     | -    |
| 上手难度       | 难    | 低      | 中            | 难     | 难   |

#### 状态管理Redux
Redux 是一个使用叫作"actions"的事件去管理和更新应用状态的模式和工具库。它以集中式Store（centralized store）的方式对整个应用中使用的状态进行集中管理，其规则确保状态只能以可预测的方式更新。

State驱动View更新， 用户操作View触发Action，再通过Action来更新State
- 单一数据源
- State是只读
- 使用纯函数来执行修改

##### Action
具有type字段的普通javaScript对象，可以将action视为描述应用程序中发生了什么的事件

##### Reducer
纯函数，接收当前的state和一个action对象，必要时决定如何更新状态，并返回新状态

##### Store
Redux的核心，整合action和reducer，存放应用的State

### ReactNative企业级
**Ctrip ReactNative**
- 方便业务接入
- 功能增强（API、组件）
- 运行性能优化、稳定性优化
- 覆盖从文档、工具、开发框架、发布、监控/性能报表全链路

https://github.com/ctripcorp/CRN

## 小程序开发